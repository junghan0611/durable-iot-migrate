# NOW — **중간 폼이 성립하는가.** 남의 실물 레시피로 증명한다

- **Question**: 서로 다른 플랫폼의 **범용 레시피**를 하나의 중간 폼(Expr IR)으로
  **손실 없이 담고 되돌릴 수 있는가.** 그것만 본다.
- **왜 이것부터인가 (2026-07-28 GLG)**: *"이거 자체를 뭔가 아이디어 내서 하면 검증 못 해."*
  우리가 문법을 발명하는 순간 **채점 기준이 사라진다** — 우리 예제로 우리 IR을 통과시키는 건
  증명이 아니다. 그래서 코퍼스는 **남이 쓴 실물 automation**이고, 점수는 **왕복**으로 낸다.
  IR을 늘릴지 말지도 우리 취향이 아니라 **코퍼스 빈도**가 정한다.
- **Stem**: 중간 폼이 성립하면 그 위에 place(장소)와 실행이 올라간다. 성립을 못 보이면
  그 위층은 전부 모래 위다. **place·런타임 배치는 이 칸이 닫힌 뒤에 연다.**
- **Current**: 문서·구성 전환 완료(`CHANGELOG.md` Unreleased). Clojure 코드 무변경 —
  **19 tests / 109 assertions, 0 failures** (flake에서 temporal-cli 제거 후 재확인).
  파서 5종은 있고, **에미터는 하나도 없다.** 그래서 아직 왕복을 못 잰다.
- **Blocker**: 없다. 아래 V1이 곧 다음 한 걸음이다.

## 검증 등급 — 무엇을 검증했다고 말할 수 있는가

먼저 좌표계를 박는다. 이게 없으면 *"잘 되는 것 같다"* 밖에 못 말한다.

| 등급 | 주장 | 어떻게 재나 |
|---|---|---|
| **G0 파싱** | 읽힌다 | 예외 없이 파서를 통과 |
| **G1 표현** | IR로 **손실 없이** 담긴다 | `unsupported` 0건 |
| **G2 왕복** | 되돌려도 같다 | `parse → emit → parse` 가 `structural-equiv?` |
| **G3 문법 유효** | 대상 플랫폼이 받아준다 | emit 결과가 그 플랫폼 스키마 통과 |
| **G4 행동 동치** | 실제로 같게 동작한다 | **범위 밖. 지금 주장하지 않는다** |

**이번 칸의 목표는 G2다.** G3는 그다음, G4는 실기 없이 주장하지 않는다.
등급을 섞어 말하지 않는다 — "왕복 됩니다"는 G2를 잰 뒤에만 하는 말이다.

## 다음 한 걸음 — V1~V4 (순서대로)

| # | 할 일 | 닫힘 기준 |
|---|---|---|
| **V1** | **코퍼스 확보.** 남이 쓴 실물 automation을 플랫폼별로 모은다. 우리가 쓴 예제는 코퍼스가 **아니다** | 플랫폼당 최소 20건, 출처·라이선스 기록. HA 우선 |
| **V2** | **HA 에미터.** `Expr → HA YAML`. 파서의 역함수 | 코퍼스 HA 건에 대해 `parse → emit → parse` 실행 가능 |
| **V3** | **fidelity 측정기.** 등급별 점수를 내는 러너 | G0/G1/G2 비율 + **미지원 구문 빈도 순위** 출력 |
| **V4** | **판독.** 숫자를 읽고 다음 칸을 정한다 | 빈도 상위 미지원 구문이 다음에 열 op을 지목 |

V3의 산출물이 이 칸의 진짜 결과물이다:

```
HA corpus: 47 recipes
  G0 파싱    47/47 (100%)
  G1 표현    31/47  (66%)
  G2 왕복    28/31  (90%)

미지원 빈도 순위:
  1. template condition      9건   ← 의도적으로 안 연다 (임의 코드 평가)
  2. wait_for_trigger        6건
  3. repeat/until            4건
  ...
```

**이 표가 나오면 이 칸은 닫힌다.** 숫자가 나쁘게 나와도 닫힌다 —
*"중간 폼이 실물의 66%를 담는다"* 는 **검증된 사실**이고, 그게 이 칸이 원한 것이다.

## 발명 금지 규율 (이 칸의 핵심)

1. **코퍼스는 남의 것이다.** 우리가 쓴 예제로 커버리지를 올리지 않는다.
   기존 테스트의 예제들은 회귀 테스트로 남고, **점수에는 안 들어간다.**
2. **op은 빈도가 연다.** *"이 op이 있으면 좋겠다"* 로 IR을 늘리지 않는다.
   코퍼스에서 N건 이상 나온 구문만 후보가 된다.
3. **못 담은 것은 센다.** 조용히 근사하지 않는다. 이름과 건수를 남긴다 —
   그 목록이 다음 칸의 입력이다.
4. **의도적으로 안 여는 것은 그렇게 적는다.** `template`처럼 열면 중심이 무너지는 것은
   빈도가 1등이어도 **안 연다.** 그건 실패가 아니라 경계다(`docs/boundaries.md`).
5. **등급을 섞지 않는다.** G2를 재고 G3를 말하지 않는다.

## 열린 결정 — 이 칸을 여는 데 필요한 것

| # | 결정 | 우리 권고 |
|---|---|---|
| **C1** | **코퍼스를 어디에 두나.** 남의 automation을 공개 리포에 벤더링하면 라이선스 문제 | 원본은 `office/`(gitignore)에 두고, 리포에는 **출처 URL + 해시 + fetch 스크립트 + 측정 결과**만. 재배포 가능한 것(공식 문서 예제 등)만 `corpus/`에 커밋 |
| **C2** | **첫 에미터 대상.** HA 하나만 먼저인가, 파서가 있는 5종 전부인가 | **HA 하나.** 왕복 러너를 먼저 세우고 나머지는 복제. 5종 동시는 러너가 없는 상태에서 다섯 배 짐 |
| **C3** | **왕복 판정 기준.** `structural-equiv?` 로 충분한가, 값 매핑까지 봐야 하나 | 같은 플랫폼 왕복은 **`structural-equiv?`**(값 매핑이 개입할 이유가 없다). `equiv?` + mapper는 **교차 플랫폼** 비교용으로 남긴다 |

## 뒤로 미룬 것 (이 칸이 닫힌 뒤)

- **place / area** — 방·거실·섹터. 코퍼스에서 area 참조가 실제로 얼마나 쓰이는지 **세고 나서** 연다.
  (HA에는 area가 실재하므로 근거는 코퍼스가 준다. 지금 열면 그것도 발명이다.)
- **런타임 배치** — 서버/허브/둘 다. 중간 폼이 성립해야 실행 얘기가 의미를 갖는다.
- **bounded 컴파일** — 제약은 `docs/boundaries.md`에 문서로 박혀 있다. 코드는 그다음.
- **리포 이름** — `durable-iot-migrate`는 옛 축을 가리킨다. 지금 바꿔도 되고 나중도 된다.
- **`minimal-iot-core` 리포 처리** — 층이 다르다(device core). 아카이브 후보이나 급하지 않다.

## GLG가 닫아준 것 (2026-07-28 구술)

- **첫 칸 = 중간 폼의 호환성 증명.** 아이디어로 만들면 검증할 수 없다. 실물로 잰다.
- **워크플로 엔진(Temporal) 안 쓴다.** *"이런 건 많으니까."* `flake.nix`에서 제거.
- **HA는 호환 대상이지 포크 대상이 아니다.** 호환은 가장자리, 중심은 작게.
- **beads 안 쓴다.** 열린 것은 이 파일, 닫힌 것은 `CHANGELOG.md`.

## 작은 빚

- GitHub 리포 description이 아직 옛 축이다 (`Temporal · Saga · Doltgres`).
- `tmp/temporal-clojure-sdk` 로컬 클론이 남아 있다(gitignore라 리포엔 영향 없음). 정리해도 된다.
- `ROADMAP.md` 없음 — **의도적.** V4 판독이 나오면 그때 쓴다. 지금 쓰면 빈 로드맵이 부채다.
- 태그 없음 — 첫 CalVer 스냅샷은 **V3 측정표가 나오면**.
